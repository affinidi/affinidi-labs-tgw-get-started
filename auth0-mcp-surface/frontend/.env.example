# Base URL of the FastAPI backend (two-server dev mode). Exposed to the browser
# (PUBLIC_ prefix required by Astro/Vite). Single-origin builds override this to "" (relative).
PUBLIC_BACKEND_URL=http://localhost:8642
