# ── Google OAuth (caller context) ─────────────────────────────────────────────
# The user authenticates with Google; the Trust Gateway verifies this JWT and
# uses it as the caller context. This is UNCHANGED from the original demo.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth callback (must match the redirect URI registered in Google Cloud Console).
# Leave unset to use the default: http://localhost:8642/api/auth/callback
# When PUBLIC_BASE_URL is set, the callback is derived from it automatically.
REDIRECT_URI=http://localhost:8642/api/auth/callback

# ── Trust Gateway ──────────────────────────────────────────────────────────────
# The Affinidi Trust Gateway MCP surface endpoint. The gateway verifies the
# Google JWT (caller context) and delegates Auth0 credentials to the upstream
# MCP server on demand (credential delegation). Auth0 client credentials live in
# the Trust Gateway's own secret store — NOT here.
GATEWAY_URL=

# ── Public exposure (ngrok / Codespaces) ────────────────────────────────────
# Single knob for a public URL. When set, the OAuth callback and post-login
# redirect use it, and it is added to CORS. Because the backend serves the
# frontend (single origin), this is all you need.
#   e.g. PUBLIC_BASE_URL=https://<name>-8642.app.github.dev  (Codespaces)
#        PUBLIC_BASE_URL=https://<subdomain>.ngrok-free.app  (ngrok)
PUBLIC_BASE_URL=

# ── Frontend origin (two-server dev only) ─────────────────────────────────────
# Only relevant when running the Astro dev server separately (make dev). In
# single-origin (make local-up / Docker) this defaults to the server's own origin.
FRONTEND_URL=http://localhost:5137

# ── Server ─────────────────────────────────────────────────────────────────────
PORT=8642

# Session cookie signing secret — set a stable value so sessions survive
# restarts (defaults to a fresh random value each boot otherwise).
SESSION_SECRET=change-me-to-a-random-secret
