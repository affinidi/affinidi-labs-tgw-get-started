# Local Prometheus config — contains your real Agent Gateway hostnames.
# Recreate it with `./run.sh <TGW_HOST>` or by copying prometheus.yml.template.
prometheus.yml

# Generated Grafana dashboards — created from dashboard-template.json,
# one per Agent Gateway. Keep the template, ignore the renders.
grafana/provisioning/dashboards/tgw-*.json
